editing the file after initial commit

this is second time commit post adding remote registry

adding these lines in online git hug remote reposity to test the reverse sync

super it syncred trying now sync from local to git repo